@{
    ViewData["Title"] = "TagHelper 教學";
}

<div class="container">
    <h1 class="mb-4">
        <i class="bi bi-tag"></i> ASP.NET Core TagHelper 教學
    </h1>

    <div class="alert alert-info">
        <strong>什麼是 TagHelper？</strong><br />
        TagHelper 是 ASP.NET Core 提供的功能，讓您可以在 Razor 視圖中使用類似 HTML 的語法來產生伺服器端程式碼。
        它們使 Razor 視圖更加易讀，並提供 IntelliSense 支援。
    </div>

    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            @TempData["Message"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- 目錄 -->
    <div class="card mb-4">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0">📚 教學目錄</h5>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-4">
                    <ul class="list-unstyled">
                        <li><a asp-fragment="anchor-taghelper">1. Anchor TagHelper (連結)</a></li>
                        <li><a asp-fragment="form-taghelper">2. Form TagHelper (表單)</a></li>
                        <li><a asp-fragment="input-taghelper">3. Input TagHelper (輸入欄位)</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <ul class="list-unstyled">
                        <li><a asp-fragment="select-taghelper">4. Select TagHelper (下拉選單)</a></li>
                        <li><a asp-fragment="validation-taghelper">5. Validation TagHelper (驗證)</a></li>
                        <li><a asp-fragment="label-taghelper">6. Label TagHelper (標籤)</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <ul class="list-unstyled">
                        <li><a asp-fragment="environment-taghelper">7. Environment TagHelper (環境)</a></li>
                        <li><a asp-fragment="cache-taghelper">8. Cache TagHelper (快取)</a></li>
                        <li><a asp-fragment="image-taghelper">9. Image TagHelper (圖片)</a></li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

    <!-- 1. Anchor TagHelper -->
    <section id="anchor-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">1. Anchor TagHelper (連結)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                <code>&lt;a&gt;</code> TagHelper 用於產生連結，可以使用 <code>asp-controller</code>、<code>asp-action</code>、
                <code>asp-route-*</code> 等屬性來動態產生 URL。
            </p>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>&lt;a asp-controller="Home" asp-action="Index"&gt;首頁&lt;/a&gt;
&lt;a asp-controller="Books" asp-action="Details" asp-route-id="1"&gt;查看書籍&lt;/a&gt;
&lt;a asp-action="Privacy" asp-fragment="section1"&gt;隱私權政策(asp-fragment=html的 錨點)&lt;/a&gt;</code></pre>

            <h5>🎯 實際範例</h5>
            <div class="bg-light p-3 rounded mb-3">
                <ul>
                    <li>
                        <a asp-controller="Home" asp-action="Index" class="btn btn-sm btn-outline-primary">
                            首頁 (asp-controller="Home" asp-action="Index")
                        </a>
                    </li>
                    <li class="mt-2">
                        <a asp-controller="Books" asp-action="Index" class="btn btn-sm btn-outline-success">
                            書籍列表 (asp-controller="Books" asp-action="Index")
                        </a>
                    </li>
                    <li class="mt-2">
                        <a asp-controller="Home" asp-action="Privacy" class="btn btn-sm btn-outline-info">
                            隱私權政策 (asp-action="Privacy")
                        </a>
                    </li>
                    <li class="mt-2">
                        <a asp-controller="Books" asp-action="Update" asp-route-id="1"
                            class="btn btn-sm btn-outline-warning">
                            編輯書籍 ID=1 (asp-route-id="1")
                        </a>
                    </li>
                </ul>
            </div>

            <div class="alert alert-warning">
                <strong>提示：</strong>使用 TagHelper 的好處是，當路由變更時，連結會自動更新，不需要手動修改每個 URL。
            </div>
        </div>
    </section>

    <!-- 2. Form TagHelper -->
    <section id="form-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">2. Form TagHelper (表單)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                <code>&lt;form&gt;</code> TagHelper 用於建立表單，會自動產生 <code>action</code> 屬性和防偽造 Token。
                使用 <code>asp-controller</code>、<code>asp-action</code> 和 <code>asp-antiforgery</code> 屬性。
            </p>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>&lt;form asp-controller="TagHelperTutorial" asp-action="FormSubmit" method="post"&gt;
    &lt;!-- 表單內容 --&gt;
    &lt;button type="submit"&gt;提交&lt;/button&gt;
&lt;/form&gt;</code></pre>

            <h5>🎯 實際範例</h5>
            <div class="bg-light p-3 rounded">
                <form asp-controller="TagHelperTutorial" asp-action="FormSubmit" method="post" class="row g-3">
                    <div class="col-md-4">
                        <label class="form-label">使用者名稱</label>
                        <input type="text" name="username" class="form-control" placeholder="請輸入使用者名稱" required />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Email</label>
                        <input type="email" name="email" class="form-control" placeholder="請輸入 Email" required />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">國家</label>
                        <select name="country" class="form-select" asp-items="@ViewBag.Countries">
                            <option value="">請選擇國家</option>
                        </select>
                    </div>
                    <div class="col-12">
                        <button type="submit" class="btn btn-primary">
                            <i class="bi bi-send"></i> 提交表單
                        </button>
                    </div>
                </form>
            </div>

            <div class="alert alert-info mt-3">
                <strong>注意：</strong>Form TagHelper 預設會自動產生 Anti-Forgery Token（CSRF 防護），
                搭配 <code>[ValidateAntiForgeryToken]</code> 屬性使用。
            </div>
        </div>
    </section>

    <!-- 3. Input TagHelper -->
    <section id="input-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">3. Input TagHelper (輸入欄位)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                <code>&lt;input&gt;</code> TagHelper 使用 <code>asp-for</code> 屬性來綁定 Model 屬性，
                會自動產生正確的 <code>name</code>、<code>id</code>、<code>type</code> 和驗證屬性。
            </p>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>@@model BookViewModel

&lt;input asp-for="Title" class="form-control" /&gt;
&lt;input asp-for="Price" class="form-control" /&gt;
&lt;input asp-for="PublishDate" class="form-control" /&gt;</code></pre>

            <h5>🎯 實際範例（無 Model 綁定）</h5>
            <div class="bg-light p-3 rounded">
                <div class="row g-3">
                    <div class="col-md-4">
                        <label class="form-label">文字輸入</label>
                        <input type="text" class="form-control" placeholder="type='text'" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">數字輸入</label>
                        <input type="number" class="form-control" placeholder="type='number'" min="0" max="100" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">日期輸入</label>
                        <input type="date" class="form-control" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">Email 輸入</label>
                        <input type="email" class="form-control" placeholder="type='email'" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">密碼輸入</label>
                        <input type="password" class="form-control" placeholder="type='password'" />
                    </div>
                    <div class="col-md-4">
                        <label class="form-label">範圍輸入</label>
                        <input type="range" class="form-range" min="0" max="100" />
                    </div>
                </div>
            </div>

            <div class="alert alert-success mt-3">
                <strong>優點：</strong>使用 <code>asp-for</code> 時，Input TagHelper 會根據 Model 屬性的資料類型
                自動設定正確的 <code>type</code> 屬性（例如 DateTime 會產生 date 類型）。
            </div>
        </div>
    </section>

    <!-- 4. Select TagHelper -->
    <section id="select-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">4. Select TagHelper (下拉選單)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                <code>&lt;select&gt;</code> TagHelper 使用 <code>asp-for</code> 綁定選中的值，
                <code>asp-items</code> 綁定選項列表（<code>IEnumerable&lt;SelectListItem&gt;</code>）。
            </p>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>// Controller
ViewBag.Countries = new List&lt;SelectListItem&gt;
{
    new SelectListItem { Value = "TW", Text = "台灣" },
    new SelectListItem { Value = "JP", Text = "日本" }
};

// View
&lt;select asp-for="Country" asp-items="@@ViewBag.Countries" class="form-select"&gt;
    &lt;option value=""&gt;請選擇&lt;/option&gt;
&lt;/select&gt;</code></pre>

            <h5>🎯 實際範例</h5>
            <div class="bg-light p-3 rounded">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">選擇國家</label>
                        <select class="form-select" asp-items="@ViewBag.Countries">
                            <option value="">請選擇國家</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">選擇類別</label>
                        <select class="form-select" asp-items="@ViewBag.Categories">
                            <option value="">請選擇類別</option>
                        </select>
                    </div>
                </div>
            </div>

            <div class="alert alert-info mt-3">
                <strong>提示：</strong>您也可以使用 <code>Enum</code> 搭配
                <code>Html.GetEnumSelectList&lt;MyEnum&gt;()</code> 來產生選項。
            </div>
        </div>
    </section>

    <!-- 5. Validation TagHelper -->
    <section id="validation-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">5. Validation TagHelper (驗證)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                驗證 TagHelper 用於顯示驗證錯誤訊息。主要有兩個：
            </p>
            <ul>
                <li><code>&lt;span asp-validation-for="PropertyName"&gt;</code> - 顯示單一欄位的驗證錯誤</li>
                <li><code>&lt;div asp-validation-summary="All"&gt;</code> - 顯示所有驗證錯誤摘要</li>
            </ul>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>&lt;!-- 單一欄位驗證訊息 --&gt;
&lt;input asp-for="Email" class="form-control" /&gt;
&lt;span asp-validation-for="Email" class="text-danger"&gt;&lt;/span&gt;

&lt;!-- 驗證摘要 --&gt;
&lt;div asp-validation-summary="All" class="text-danger"&gt;&lt;/div&gt;

&lt;!-- ValidationSummary 選項 --&gt;
&lt;!-- None: 不顯示 --&gt;
&lt;!-- ModelOnly: 只顯示 Model 層級錯誤 --&gt;
&lt;!-- All: 顯示所有錯誤 --&gt;</code></pre>

            <h5>🎯 實際範例</h5>
            <div class="bg-light p-3 rounded">
                <form class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">必填欄位</label>
                        <input type="text" class="form-control" required placeholder="此欄位為必填" />
                        <span class="text-danger">* 請輸入此欄位</span>
                    </div>
                    <div class="col-md-6">
                        <label class="form-label">Email 格式驗證</label>
                        <input type="email" class="form-control" placeholder="請輸入有效的 Email" />
                        <span class="text-danger">* 請輸入有效的 Email 格式</span>
                    </div>
                </form>
            </div>

            <div class="alert alert-warning mt-3">
                <strong>記得：</strong>要啟用客戶端驗證，需要引入驗證腳本：
                <code>&lt;partial name="_ValidationScriptsPartial" /&gt;</code>
            </div>
        </div>
    </section>

    <!-- 6. Label TagHelper -->
    <section id="label-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">6. Label TagHelper (標籤)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                <code>&lt;label&gt;</code> TagHelper 使用 <code>asp-for</code> 屬性來自動產生 <code>for</code> 屬性
                和顯示文字（從 <code>[Display(Name = "...")]</code> 屬性取得）。
            </p>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>// Model
public class BookViewModel
{
    [Display(Name = "書籍名稱")]
    public string Title { get; set; }
}

// View
&lt;label asp-for="Title"&gt;&lt;/label&gt;
&lt;!-- 產生: &lt;label for="Title"&gt;書籍名稱&lt;/label&gt; --&gt;</code></pre>

            <h5>🎯 實際範例</h5>
            <div class="bg-light p-3 rounded">
                <div class="mb-3">
                    <label class="form-label fw-bold">書籍名稱</label>
                    <input type="text" class="form-control" placeholder="使用 [Display(Name='書籍名稱')] 產生" />
                </div>
                <div class="mb-3">
                    <label class="form-label fw-bold">出版日期</label>
                    <input type="date" class="form-control" />
                </div>
            </div>
        </div>
    </section>

    <!-- 7. Environment TagHelper -->
    <section id="environment-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">7. Environment TagHelper (環境)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                <code>&lt;environment&gt;</code> TagHelper 根據執行環境（Development、Staging、Production）
                來決定是否渲染內容。常用於載入不同的 CSS/JS 檔案。
            </p>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>&lt;environment include="Development"&gt;
    &lt;!-- 開發環境載入未壓縮版本 --&gt;
    &lt;script src="~/lib/jquery/jquery.js"&gt;&lt;/script&gt;
&lt;/environment&gt;

&lt;environment exclude="Development"&gt;
    &lt;!-- 正式環境載入壓縮版本 --&gt;
    &lt;script src="~/lib/jquery/jquery.min.js"&gt;&lt;/script&gt;
&lt;/environment&gt;

&lt;environment names="Staging,Production"&gt;
    &lt;!-- 多個環境 --&gt;
&lt;/environment&gt;</code></pre>

            <h5>🎯 實際範例</h5>
            <div class="bg-light p-3 rounded">
                <environment include="Development">
                    <div class="alert alert-warning">
                        <strong>🔧 開發環境</strong> - 您目前在開發環境中！
                    </div>
                </environment>
                <environment exclude="Development">
                    <div class="alert alert-success">
                        <strong>🚀 正式環境</strong> - 您目前在正式環境中！
                    </div>
                </environment>
                <p class="text-muted">（上方會根據目前的執行環境顯示不同的訊息）</p>
            </div>
        </div>
    </section>

    <!-- 8. Cache TagHelper -->
    <section id="cache-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">8. Cache TagHelper (快取)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                <code>&lt;cache&gt;</code> TagHelper 可以快取 Razor 視圖的部分內容，
                減少重複運算，提升效能。
            </p>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>&lt;!-- 基本快取（預設 20 分鐘） --&gt;
&lt;cache&gt;
    目前時間：@@DateTime.Now
&lt;/cache&gt;

&lt;!-- 設定過期時間 --&gt;
&lt;cache expires-after="@@TimeSpan.FromMinutes(5)"&gt;
    這個內容會快取 5 分鐘
&lt;/cache&gt;

&lt;!-- 根據參數變化快取 --&gt;
&lt;cache vary-by-route="id"&gt;
    根據路由參數 id 快取不同版本
&lt;/cache&gt;

&lt;!-- 可用的 vary-by 屬性 --&gt;
&lt;!-- vary-by-route, vary-by-query, vary-by-cookie, vary-by-header, vary-by-user --&gt;</code></pre>

            <h5>🎯 實際範例</h5>
            <div class="bg-light p-3 rounded">
                <div class="row">
                    <div class="col-md-6">
                        <h6>未快取的時間：</h6>
                        <p class="text-primary fs-5">@DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss.fff")</p>
                        <small class="text-muted">每次重新整理都會更新</small>
                    </div>
                    <div class="col-md-6">
                        <h6>快取的時間（5 分鐘）：</h6>
                        <cache expires-after="@TimeSpan.FromMinutes(5)">
                            <p class="text-success fs-5">@DateTime.Now.ToString("yyyy-MM-dd HH:mm:ss.fff")</p>
                        </cache>
                        <small class="text-muted">5 分鐘內重新整理會保持不變</small>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- 9. Image TagHelper -->
    <section id="image-taghelper" class="card mb-4">
        <div class="card-header bg-success text-white">
            <h4 class="mb-0">9. Image TagHelper (圖片)</h4>
        </div>
        <div class="card-body">
            <h5>📖 說明</h5>
            <p>
                <code>&lt;img&gt;</code> TagHelper 的 <code>asp-append-version</code> 屬性
                會在圖片 URL 後加上版本戳記，確保瀏覽器不會使用過期的快取圖片。
            </p>

            <h5>💻 語法</h5>
            <pre class="bg-light p-3 rounded"><code>&lt;!-- 加上版本戳記 --&gt;
&lt;img src="~/images/logo.png" asp-append-version="true" /&gt;

&lt;!-- 產生結果類似: --&gt;
&lt;img src="/images/logo.png?v=abc123..." /&gt;</code></pre>

            <h5>🎯 實際範例</h5>
            <div class="bg-light p-3 rounded">
                <p>圖片 TagHelper 會自動在 URL 後加上檔案的 hash 值：</p>
                <pre
                    class="bg-dark text-light p-2 rounded">&lt;img src="~/imgs/sample.jpg" asp-append-version="true" /&gt;</pre>
                <p class="text-muted mt-2">
                    這個功能稱為 <strong>Cache Busting</strong>，當圖片更新時，hash 值會改變，
                    瀏覽器就會重新載入新圖片。
                </p>
            </div>
        </div>
    </section>

    <!-- 其他常用 TagHelper -->
    <section class="card mb-4">
        <div class="card-header bg-info text-white">
            <h4 class="mb-0">📌 其他常用 TagHelper</h4>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th>TagHelper</th>
                            <th>用途</th>
                            <th>範例</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td><code>&lt;partial&gt;</code></td>
                            <td>引入部分視圖</td>
                            <td><code>&lt;partial name="_LoginPartial" /&gt;</code></td>
                        </tr>
                        <tr>
                            <td><code>&lt;link&gt;</code></td>
                            <td>CSS 連結（支援 asp-append-version）</td>
                            <td><code>&lt;link rel="stylesheet" href="~/css/site.css" asp-append-version="true" /&gt;</code>
                            </td>
                        </tr>
                        <tr>
                            <td><code>&lt;script&gt;</code></td>
                            <td>JavaScript 引入（支援 asp-append-version）</td>
                            <td><code>&lt;script src="~/js/site.js" asp-append-version="true"&gt;&lt;/script&gt;</code>
                            </td>
                        </tr>
                        <tr>
                            <td><code>&lt;textarea&gt;</code></td>
                            <td>多行文字輸入（支援 asp-for）</td>
                            <td><code>&lt;textarea asp-for="Description"&gt;&lt;/textarea&gt;</code></td>
                        </tr>
                        <tr>
                            <td><code>&lt;component&gt;</code></td>
                            <td>渲染 Blazor 元件</td>
                            <td><code>&lt;component type="typeof(Counter)" render-mode="ServerPrerendered" /&gt;</code>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </section>

    <!-- 總結 -->
    <section class="card mb-4 border-primary">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">🎓 總結</h4>
        </div>
        <div class="card-body">
            <h5>TagHelper 的優點：</h5>
            <ul>
                <li>✅ 語法更接近 HTML，易於閱讀和維護</li>
                <li>✅ 提供完整的 IntelliSense 支援</li>
                <li>✅ 自動處理 URL 產生和路由變更</li>
                <li>✅ 內建安全功能（如 Anti-Forgery Token）</li>
                <li>✅ 支援 Model 綁定和驗證</li>
                <li>✅ 可以自訂 TagHelper 擴充功能</li>
            </ul>

            <h5>啟用 TagHelper：</h5>
            <p>在 <code>_ViewImports.cshtml</code> 中加入：</p>
            <pre class="bg-light p-3 rounded"><code>@@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers</code></pre>

            <div class="text-center mt-4">
                <a asp-controller="Home" asp-action="Index" class="btn btn-primary btn-lg">
                    <i class="bi bi-house"></i> 返回首頁
                </a>
                <a asp-controller="Books" asp-action="Index" class="btn btn-success btn-lg ms-2">
                    <i class="bi bi-book"></i> 查看書籍範例
                </a>
            </div>
        </div>
    </section>
</div>

@section Scripts {
    <script>
        // 平滑滾動到錨點
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
}